services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    volumes:
      - ./ollama:/root/.ollama
    ports:
      # Host port is configurable so it never collides with another local
      # service. Defaults to the standard Ollama port.
      - "${OLLAMA_PORT:-11434}:11434"
    environment:
      - OLLAMA_HOST=0.0.0.0:11434
      - OLLAMA_KEEP_ALIVE=${OLLAMA_KEEP_ALIVE:-24h}
      # Allow browser-based clients (e.g. a deployed Waymark) to call the API.
      # Defaults to "*"; set OLLAMA_ORIGINS to your exact Waymark origin(s) to
      # lock it down (comma-separated).
      - OLLAMA_ORIGINS=${OLLAMA_ORIGINS:-*}
    pull_policy: missing
    tty: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "ollama list >/dev/null 2>&1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    # Uncomment the lines below if you have an NVIDIA GPU
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    volumes:
      - ./open-webui:/app/backend/data
    depends_on:
      ollama:
        condition: service_healthy
    ports:
      # Defaults to 8095 to avoid clashing with common dev servers (Waymark web
      # on 3000, Waymark p2p on 8090). Override with WEBUI_PORT in your .env.
      - "${WEBUI_PORT:-8095}:8080"
    environment:
      - OLLAMA_BASE_URL=http://ollama:11434
      # Auth can only be disabled on a fresh install (no existing users).
      # Set WEBUI_AUTH=True (and RESET_WEBUI_DATA=0) to keep accounts.
      - WEBUI_AUTH=${WEBUI_AUTH:-False}
      - ENABLE_SIGNUP=${ENABLE_SIGNUP:-False}
    restart: unless-stopped
